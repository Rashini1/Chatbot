#   Chatbot (Python + Groq API)

A simple **command-line chatbot** built in Python using the **Groq API** and the **Llama 3.1 model**.
This chatbot runs entirely in your terminal — no extra UI or web server required.

---

## 🚀 Features

* 🧠 Uses **Llama 3.1 (8B)** — a fast, open, and free-to-use language model
* 💬 Interactive chat loop (type and get instant replies)
* 🖥️ Runs directly in your terminal — no browser needed
* 🛑 Type `exit` anytime to stop the chat

---

## 🧩 Requirements

Make sure you have:

* **Python 3.8+** installed
* A **Groq API Key** (get one free at [https://console.groq.com](https://console.groq.com))

---

## ⚙️ Installation

1. **Clone or create the project folder**

   ```bash
   mkdir llama-chatbot
   cd llama-chatbot
   ```

2. **Install dependencies**

   ```bash
   pip install groq
   ```

3. **Create a Python file**
   Save your chatbot as `main.py` with the following code:

     

##  Run the Chatbot

Run the chatbot using:

```bash
python main.py
```

You’ll see:

```
🤖  Chatbot (Free via Groq): Type 'exit' to stop.

You: Hello!
🤖 Chatbot: Hi there! How can I help you today?
```

---

## 🔑 Notes

* Replace `"your_api_key_here"` with your **Groq API key**.
* To stop chatting, type `exit` and press Enter.
* You can change the model (e.g., `llama-3.1-70b-versatile`) if available.

---

## 🧠 Example Conversation

```
🤖 Llama Chatbot (Free via Groq): Type 'exit' to stop.

You: What is AI?
🤖 Chatbot: AI stands for Artificial Intelligence — the simulation of human intelligence in machines.

You: exit
🤖 Chatbot: Goodbye!
```

---

 
